import type { InterviewQuestion } from "./interviewQuestions";

export const interviewQuestionsJunior: InterviewQuestion[] = [
  {
    id: "ai-llm-junior-1",
    role: "ai-engineer",
    category: "LLMs & Prompt Engineering",
    difficulty: "Junior",
    question: "What is a token, and why does tokenization matter when working with LLMs?",
    tldr: "A token is the model's basic text unit; tokenization affects context length, cost, and how text is represented.",
    answer: "A tokenizer converts text into numeric token IDs before the model processes it. A token may be a whole word, part of a word, punctuation, or whitespace. Tokenization matters because model limits and API prices are measured in tokens, not characters or words. It can also affect multilingual performance because the same sentence may require very different token counts across languages. In practice, estimate token usage before sending long documents and use the tokenizer that matches the target model.",
    keyPoints: ["Tokens are model-specific text units", "Context limits include input and output tokens", "Token counts affect cost and latency", "Multilingual text can tokenize less efficiently"],
    pitfalls: ["Assuming one token always equals one word", "Using a tokenizer from a different model family"],
    interviewTip: "Give a short example such as 'unbelievable' being split into multiple subword tokens.",
    followUpQuestions: ["How would you count tokens before an API request?", "What happens when a prompt exceeds the context window?"],
    tags: ["tokens", "fundamentals"],
  },
  {
    id: "ai-ml-junior-2",
    role: "ai-engineer",
    category: "Machine Learning Fundamentals",
    difficulty: "Junior",
    question: "What is the difference between supervised, unsupervised, and reinforcement learning?",
    tldr: "Supervised learning uses labels, unsupervised learning finds structure without labels, and reinforcement learning learns from rewards.",
    answer: "Supervised learning learns a mapping from inputs to known labels, such as predicting churn or house prices. Unsupervised learning works without target labels and discovers structure, such as customer segments or lower-dimensional representations. Reinforcement learning trains an agent to choose actions in an environment to maximize cumulative reward. The correct choice depends on what feedback the data provides and what outcome the system must optimize.",
    keyPoints: ["Supervised: labeled examples", "Unsupervised: patterns without target labels", "Reinforcement learning: actions, states, and rewards", "Problem framing determines the learning setup"],
    pitfalls: ["Calling clustering supervised learning", "Assuming reinforcement learning is appropriate for every sequential problem"],
    interviewTip: "Give one concrete business example for each learning type.",
    followUpQuestions: ["Is self-supervised learning supervised or unsupervised?", "How would you evaluate a clustering result?"],
    tags: ["learning-types", "fundamentals"],
  },
  {
    id: "ai-dl-junior-3",
    role: "ai-engineer",
    category: "Deep Learning & Neural Networks",
    difficulty: "Junior",
    question: "What are an epoch, batch, and iteration in neural network training?",
    tldr: "An epoch is one pass over the training set, a batch is one subset, and an iteration is one parameter update.",
    answer: "A batch is the subset of training examples processed together. One iteration is a forward pass, backward pass, and optimizer update for one batch. An epoch is completed after the model has seen the entire training set once. If a dataset has 10,000 examples and the batch size is 100, one epoch contains 100 iterations. Batch size affects memory use, gradient noise, throughput, and sometimes generalization.",
    keyPoints: ["Iteration means one optimizer update", "Iterations per epoch are approximately dataset size divided by batch size", "Smaller batches use less memory", "Validation is usually run at epoch boundaries"],
    pitfalls: ["Using epoch and iteration as synonyms", "Comparing epoch counts across differently sized datasets without context"],
    interviewTip: "Use a numeric example to make the distinction unambiguous.",
    followUpQuestions: ["How does batch size affect learning rate?", "What is gradient accumulation?"],
    tags: ["training", "fundamentals"],
  },
  {
    id: "de-sql-junior-1",
    role: "data-engineer",
    category: "SQL & Databases",
    difficulty: "Junior",
    question: "What is a primary key, and how is it different from a foreign key?",
    tldr: "A primary key uniquely identifies a row; a foreign key references a key in another table to preserve relationships.",
    answer: "A primary key is a column or set of columns whose value is unique and non-null for every row. A foreign key stores a value that references a primary or unique key in another table. Primary keys provide row identity, while foreign keys enforce referential integrity between tables. For example, customers.id can be a primary key and orders.customer_id a foreign key referencing it.",
    keyPoints: ["Primary keys are unique and non-null", "Foreign keys enforce valid relationships", "Composite keys use more than one column", "Indexes and constraints are related but not identical"],
    pitfalls: ["Assuming a foreign key is automatically indexed in every database", "Using mutable business data as a primary key without considering change"],
    interviewTip: "Draw a customers-to-orders example and explain what happens when a customer is deleted.",
    followUpQuestions: ["What is referential integrity?", "When would you use a composite key?"],
    tags: ["sql", "keys", "fundamentals"],
  },
  {
    id: "de-sql-junior-2",
    role: "data-engineer",
    category: "SQL & Databases",
    difficulty: "Junior",
    question: "What is the difference between GROUP BY and DISTINCT?",
    tldr: "DISTINCT removes duplicate result rows; GROUP BY forms groups so aggregate calculations can be performed.",
    answer: "DISTINCT returns unique combinations of the selected columns. GROUP BY also produces one row per unique grouping key, but its main purpose is to calculate aggregates such as COUNT, SUM, AVG, MIN, or MAX for each group. If no aggregate is used, GROUP BY can resemble DISTINCT, but DISTINCT communicates deduplication more clearly.",
    keyPoints: ["DISTINCT removes duplicate result rows", "GROUP BY supports aggregation", "Both operate after FROM and WHERE", "Choose the construct that communicates intent"],
    pitfalls: ["Selecting non-grouped, non-aggregated columns", "Using DISTINCT to hide an incorrect join that creates duplicates"],
    interviewTip: "Explain why DISTINCT can mask a data-model or join bug.",
    followUpQuestions: ["When would HAVING be used?", "How does COUNT(DISTINCT column) work?"],
    tags: ["sql", "aggregation", "fundamentals"],
  },
  {
    id: "de-pipe-junior-1",
    role: "data-engineer",
    category: "Data Pipelines & ETL",
    difficulty: "Junior",
    question: "What are the main stages of a basic data pipeline?",
    tldr: "A basic pipeline ingests data, validates and transforms it, stores the result, and monitors delivery quality.",
    answer: "A basic pipeline starts by extracting or receiving data from source systems. It validates schema and quality, transforms the data into a useful model, loads it into a destination, and records operational metadata such as row counts and timestamps. Production pipelines also need orchestration, retries, alerting, lineage, and a clear owner. The design should make failed runs safe to repeat.",
    keyPoints: ["Ingest, validate, transform, store, monitor", "Retries require idempotent tasks", "Quality checks belong throughout the pipeline", "Ownership and observability are production requirements"],
    pitfalls: ["Treating monitoring as optional", "Mixing extraction and destructive transformations in one unrecoverable step"],
    interviewTip: "Describe both the happy path and what happens when one stage fails.",
    followUpQuestions: ["What makes a task idempotent?", "Where would you quarantine invalid records?"],
    tags: ["pipelines", "etl", "fundamentals"],
  },
  {
    id: "de-bd-junior-1",
    role: "data-engineer",
    category: "Big Data & Cloud",
    difficulty: "Junior",
    question: "Why are columnar file formats such as Parquet useful for analytics?",
    tldr: "Columnar formats store values by column, enabling compression and reading only the fields an analytical query needs.",
    answer: "Analytical queries often scan a few columns across many rows. Parquet stores values from each column together, so query engines can skip unneeded columns and compress similar values efficiently. It also stores metadata such as min and max values that can help skip row groups. This usually reduces storage, network transfer, and scan cost compared with row-oriented text formats such as CSV.",
    keyPoints: ["Column pruning reduces I/O", "Typed schemas are safer than CSV", "Compression is efficient for similar adjacent values", "Parquet is a file format, not a database"],
    pitfalls: ["Creating many tiny Parquet files", "Assuming Parquet alone provides transactions or concurrent update safety"],
    interviewTip: "Contrast a SELECT of two columns from a wide table stored as CSV versus Parquet.",
    followUpQuestions: ["What is predicate pushdown?", "How does partitioning complement Parquet?"],
    tags: ["parquet", "columnar", "fundamentals"],
  },
  {
    id: "de-sys-junior-1",
    role: "data-engineer",
    category: "System Design",
    difficulty: "Junior",
    question: "What does data freshness mean, and how would you monitor it?",
    tldr: "Freshness measures how recently expected data arrived and whether it is available by the time consumers need it.",
    answer: "Data freshness is the delay between when data is created or expected and when it becomes available for use. Monitor it with timestamps such as source event time, ingestion time, and the latest successfully processed partition. Define an expectation, for example: the daily sales table must be updated by 08:00. Alert when the latest timestamp or successful run exceeds that threshold, and include the affected dataset and owner in the alert.",
    keyPoints: ["Freshness is a measurable consumer expectation", "Use event, ingestion, and processing timestamps", "Thresholds should reflect business needs", "Alerts need ownership and actionable context"],
    pitfalls: ["Equating a successful job with fresh data", "Alerting without naming the delayed dataset or owner"],
    interviewTip: "State a concrete freshness target and the exact signal that would trigger an alert.",
    followUpQuestions: ["How is freshness different from completeness?", "What is an SLO for a data product?"],
    tags: ["freshness", "monitoring", "fundamentals"],
  },
];
